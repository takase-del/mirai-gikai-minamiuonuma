/**
 * サービスの識別情報。
 *
 * FORK_GUIDELINES.md「1. サービス名称」に基づき、本家「みらい議会」と
 * 区別できる名称をここ1か所で定義する。画面ごとに直書きすると、名称変更の
 * たびに取りこぼしが出て本家と混同される表示が残るため、必ずここから引く。
 *
 * 例外は `web/public/manifest.json`。静的JSONで import できないため同じ値を
 * 別に持っている。名称を変えるときは manifest.json も合わせて直すこと。
 */
export const SITE_NAME = "みらい議会＠南魚沼市";

/**
 * 記号が使えない場所向けの短縮名。
 *
 * ハッシュタグは「＠」で切れてしまい、本家と同じ `#みらい議会` として
 * 拡散されてしまうため、記号を含まない形を用意する。
 */
export const SITE_SHORT_NAME = "みらい議会南魚沼市";

/** 一覧やカードの説明文に使う一文。 */
export const SITE_DESCRIPTION =
  "国会で今どんな法案が検討されているか、わかりやすく伝えるプラットフォーム";

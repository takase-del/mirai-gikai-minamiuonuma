/**
 * 機能のオン・オフ。
 *
 * fork 元にある機能のうち、本サイトの運営体制が整うまで出さないものを
 * ここで落とす。コードは残したまま入口だけ閉じるので、方針が決まったら
 * フラグを戻すだけで再開できる。
 */
export const FEATURES = {
  /**
   * AIチャット（「AIに質問」）。
   *
   * 議案の内容を対話で解説する機能。回答の正確性を運営側で担保できる
   * 体制と、AI利用料の上限設定が固まるまで無効にしている。
   * 有効化するときは `.env` の `AI_GATEWAY_API_KEY` と
   * `CHAT_DAILY_*` / `CHAT_MONTHLY_*` のコスト上限もあわせて設定すること。
   */
  aiChat: false,
} as const;
